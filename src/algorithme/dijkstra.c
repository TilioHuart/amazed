/*
** EPITECH PROJECT, 2024
** Amazed
** File description:
** Pathfinding algorithme
*/

#include "my_macros.h"
#include "my.h"
#include "my_types.h"
#include <stdio.h>

static
int find_path_loop(char **matrix)
{
    return SUCCESS;
}

int find_path(char **matrix, int nb_robots)
{
    if (matrix == NULL)
        return display_error("Matrix NUll in algo"), FAILURE;
    return SUCCESS;
}
